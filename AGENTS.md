# AGENTS.md — Codex 开发规则

## 1. 项目目标

本仓库开发 **Business Ops Workbench（经营诊断工作台）**。

`SPEC.md` 是最高产品规格。若实现方案与 SPEC 冲突，优先遵守 SPEC；若 SPEC 存在歧义，停止扩展并说明问题，不得自行创造业务规则。

## 2. 工作方式

严格按里程碑开发：M0 → M1 → M2 → M3 → M4。

禁止：
- 未经明确指令提前实现后续里程碑。
- 因“顺便”增加不在 SPEC 中的功能。
- 将假设的嗨学业务规则写入通用代码。
- 引入不必要的大型框架。
- 为追求架构“先进”而过度设计。

## 3. 开发原则

### 3.1 配置与代码分离
优先配置化：字段映射、指标公式、维度顺序、异常阈值、UI 默认参数。

### 3.2 行业无关
不得硬编码：嗨学网、教育行业专属 KPI、具体课程名、真实员工姓名、真实部门名、真实组织结构。

Demo 只能使用虚构数据。

### 3.3 本地优先
V1 必须离线可运行。未经明确批准，不得调用外部 API、上传业务数据、引入第三方 SaaS、使用云数据库。

### 3.4 不静默修复数据
- 格式问题可清洗，但必须记录处理规则。
- 影响业务含义的问题必须警告或阻断。
- 不得把 NULL 悄悄变成 0。
- 不得悄悄删除异常行。

## 4. Python 规范

- Python 3.12+
- 使用类型标注。
- 公共函数写 docstring。
- 函数单一职责。
- I/O 与业务计算分离。
- UI 文件不承载核心计算逻辑。
- 业务逻辑放在 metrics/、diagnostics/ 等模块。
- 禁止全局可变状态。
- 文件路径使用 pathlib.Path。
- 日志使用 logging，不使用散落 print。

## 5. 数据计算规则

### 比率
- 分母为 0 → NULL
- 分母缺失 → unavailable
- 不返回 inf / -inf

### 聚合
- 金额、人数等原子指标：SUM
- 比率：先聚合分子分母，再重新计算
- 禁止简单平均比例，除非指标定义明确允许

### 缺失
未知 ≠ 0。NULL 与 0 必须严格区分。

## 6. 诊断规则

所有诊断输出必须区分：Fact、Diagnostic、Hypothesis、Recommendation。

不得将相关性写成因果。

错误：
> 抖音渠道导致收入下降。

正确：
> 收入下降主要集中在抖音渠道；该渠道有效线索率同步下降，建议优先检查线索来源和质量变化。当前数据只能支持相关性判断。

## 7. 测试要求

每个里程碑完成前运行：

```bash
pytest
```

新增核心逻辑必须有测试。

必测：
- CSV / Excel 读取
- 字段映射
- schema 校验
- 指标公式
- 0 分母
- NULL 输入
- 聚合后的比率
- 环比/周同比
- 异常检测
- 贡献分析

Demo 中注入的异常必须可被识别。

## 8. UI 规范

Streamlit 只负责呈现和交互。

要求：
- 页面异常不导致整个应用崩溃。
- 缺失指标有降级提示。
- 数据不足明确提示。
- 默认简洁，不堆图表。
- 先回答经营问题，再考虑视觉效果。

V1 页面固定为：经营总览、经营漏斗、诊断中心、异常中心、数据管理。

不得自行新增页面。

## 9. 依赖管理

新增依赖前判断：
1. 标准库能否解决？
2. 当前依赖能否解决？
3. 是否真正降低复杂度？
4. 是否影响离线运行？

## 10. 完成任务后的汇报格式

### 完成
### 文件
### 验证
### 未完成
### 风险/问题

必须包含测试结果，不要只说“完成了”。

## 11. M0 专项限制

M0 只允许：
- 目录
- pyproject.toml
- .gitignore
- .env.example
- README
- AGENTS
- SPEC
- Streamlit 最小启动页
- pytest smoke test
- logging 基础模块

严禁在 M0 实现：数据导入、DuckDB 业务表、指标引擎、异常检测、贡献分析、Demo 数据、完整 Dashboard。

## 12. 安全与隐私

后续接入真实公司数据时：
- 默认本地处理。
- 不将真实数据提交 Git。
- data/raw、staging、marts、exports 默认忽略真实数据文件。
- 不在测试、日志、截图中保存真实个人信息。
- 未经公司允许，不把真实客户/员工/订单数据上传外部 AI。

## 13. 不确定时

若遇到指标定义不明确、字段含义冲突、多粒度混杂、对真实流程存在猜测：
1. 保留原数据；
2. 标记问题；
3. 提供最小可行选择；
4. 等待确认。

不得自行“合理猜测”后写入正式逻辑。
